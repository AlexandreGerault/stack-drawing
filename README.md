# Stack drawing

## Intent

This project aims to provide a learning tool to understand what's happening, step by step, when an assembly code is being processed by the CPU.

The goal is to draw the evolution of the CPU (registers), the stack and the memory.

This is not a full emulator, it just plans to support basic instructions that are met in TPs.